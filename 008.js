/**
 * @param {string} str
 * @return {number}
 */
function myAtoi(str) {
    if(str===null||str===''||str===NaN)
        return 0;
    if(str[i].charCodeAt() >= 48 && str[i].charCodeAt() <= 57)
    for(var i=0)
    {
        if(str[i].charCodeAt() >= 48 && str[i].charCodeAt() <= 57)
    }
};